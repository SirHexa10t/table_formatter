# Experiments

Benchmark reports produced by `experiments/benchmark.sh <label>`.

File naming: `bench_<date>_<label>.txt`. The label should say what state of the
code was measured (e.g. `pre-optimization`, `post-optimization`), so
before/after pairs are easy to line up.
